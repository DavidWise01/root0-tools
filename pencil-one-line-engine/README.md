# Pencil One-Line Engine v0.1

Open `index.html` in a browser.

Signature:

```text
))((1)) 999#@ / @#999 ((1))((
```

Functional pieces: step, run, ground, audit, short-term pruning, long-pipe witness hash.
